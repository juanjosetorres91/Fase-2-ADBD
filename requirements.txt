# Fase 2 · pipeline batch + ML distribuido + MLflow
# Las versiones están FIJAS a propósito: un pipeline reproducible no puede depender
# de qué resolvió pip el día que se corrió.

pyspark==3.5.4
mlflow==2.17.2

# Motores de la Fase 1: DuckDB sigue siendo el de exploración y validación cruzada.
duckdb==1.3.2
polars==1.35.2

pandas>=2.0,<3.0
numpy>=1.26,<2.3
pyarrow>=14.0
matplotlib>=3.8

# Acceso al espejo de Kaggle (la licencia reconocida es la de Tianchi)
kagglehub>=0.3

# Utilidades
psutil>=5.9
pytest>=8.0

# Nota de entorno: Spark 3.5 soporta Java 8/11/17. Colab trae Java 11 y funciona
# sin ajustes. En JDK 21 el Arrow que empaqueta Spark 3.5 falla al convertir a
# pandas; en ese caso se corre con ADBD_ARROW=false (ver adbd/config.py).
